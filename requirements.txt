Flask==3.1.3
face-recognition==1.3.0
# We use the headless version to save space and avoid GUI dependencies
opencv-python-headless==4.13.0.92
# Force an older numpy because 2.x often breaks face_recognition/dlib compatibility
numpy==1.26.4
gunicorn
python-dotenv